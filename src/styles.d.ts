declare module "@mui/material/styles" {
  interface Theme {
    palette: {
      primary: {
        main: string;
        light: string;
      }
      secondary: {
        main: string;
        light: string;
      }
      background: {
        default: string;
      } 
    }
  }

  interface ThemeOptions {
    palette: {
      primary: {
        main: string;
        light: string;
      }
      secondary: {
        main: string;
        light: string;
      }
      background: {
        default: string;
      } 
    }
  }
}